using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UsePowerButton : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*Player p = Player.GetPlayer(GameLogic.instance.PlayerTurn.Value);

        p.CanUsePower.AddListener((sender) =>
        {
            this.gameObject.SetActive(p.CanUsePower.Value);
        });*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
